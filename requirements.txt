# --- UI ---
PyQt5>=5.15

# --- Imaging ---
Pillow>=10.0
numpy>=1.24

# --- PDF handling ---
pypdf>=4.0
PyPDF2>=3.0        # still used by a few older code paths alongside pypdf
pdf2image>=1.17    # requires poppler installed on the system separately
PyMuPDF>=1.23      # imported as `fitz`

# --- Background removal (offline, CPU) ---
torch>=2.0
transparent-background>=1.3

# --- LAN scan/share server ---
qrcode>=7.4
pyperclip>=1.8
